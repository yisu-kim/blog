import { createMuiTheme } from "@material-ui/core"

const muiTheme = createMuiTheme({
  palette: {
    primary: {
      main: `#1EB980`,
    },
    secondary: {
      main: `#FF6859`,
    },
  },
})

export default muiTheme
